name: Backend CD - Disabled Until Homologation

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deployment-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse unhomologated deployment
        run: |
          echo "Production deployment is blocked until Phase 28 is approved."
          exit 1
