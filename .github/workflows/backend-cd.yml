name: Backend CD - Production

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deployment-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Homologated deployment authorized
        run: |
          echo "Production deployment authorized. Awaiting specific Vercel/VPS hooks."
          # Trigger hooks here
