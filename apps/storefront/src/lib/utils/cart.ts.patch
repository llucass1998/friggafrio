--- apps/storefront/src/lib/utils/cart.ts
+++ apps/storefront/src/lib/utils/cart.ts
@@ -23,6 +23,12 @@
   })
 }
 
+// ============ CART HELPERS ============
+
+export const getCartItemCount = (items?: HttpTypes.StoreCartLineItem[]): number => {
+  return items?.reduce((total, item) => total + (item.quantity || 0), 0) || 0;
+}
+
 // ============ OPTIMISTIC CART ============
 
 export interface OptimisticCartItem {
