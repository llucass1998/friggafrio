console.log("Adding to cart WORKS when using the Brazil region which actually has prices configured.");
console.log("The storefront is likely defaulting to the wrong region (Denmark/Europe), which doesn't have prices, causing Medusa V2's core-flows to crash when creating the cart.");
